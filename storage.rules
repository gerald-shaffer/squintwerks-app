rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read access for all assets (videos, models, images)
    match /xweb-assets/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
